let key = parseInt(prompt(`Ingrese un numero`));
switch (key) {
     case 1:
        console.log(`Opcion uno`);
        break;
    case 2:
        console.log(`Opcion dos`);
        break;

    default:
        console.log(`Opcion incorrecta`);
        break;
}

